# ==============================================================================
# 忽略规则
#
#   ⚠ 别写 data/ 、logs/ 这种"无边界的目录名"规则！
#     Git 会把它套用到任意层级，从而把 upstream/internal/data/ 整个 Go 包
#     一起忽略掉（这个坑踩过一次，会导致构建失败）。
#     要忽略根目录时请带前导斜杠：/data/ 、/logs/
#
#   ⚠ 同理，根目录的规则**无法**反忽略子目录 .gitignore 的排除，
#     所以 upstream/.gitignore 已被移除（详见 UPSTREAM.md）。
# ==============================================================================

# ---- 环境变量与密钥（内含 APP_SECRET、数据库密码，绝不可提交）----
.env
.env.local
*.env

# ---- 运行时数据（只针对根目录）----
/data/
/logs/
*.db
*.sqlite3

# ---- 临时文件 ----
*.tar.gz
*.tgz

# ---- 编辑器与系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
__pycache__/

# ---- 工具的工作记录（本地数据，不进仓库）----
.workbuddy/
